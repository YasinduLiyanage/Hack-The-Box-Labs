MISSION - I had to connect to a target machine using telnet and find the flag in its directory.

USED TOOLS - ping, nmap, telnet

**1. Enumeration**
I used the ping command to check whether I can reach the target IP
<img width="649" height="222" alt="image" src="https://github.com/user-attachments/assets/01519c08-f0a1-40f9-8dd3-777d8e97ebcd" />

Then I used nmap to see what services and open ports are running on the target machine
<img width="893" height="259" alt="image" src="https://github.com/user-attachments/assets/fd062e3e-77fd-4119-aad0-954d5982adb9" />

We can see telnet is running on port 23/tcp. Telnet is an old remote management protocol which uses plain text during the connection.

**2.Foothold**

I connected through telnet, and it requested a username and password.

Because of configuration mistakes, there can be blank passwords even in the important accounts.

I manually brute-forced two times as admin and then root. root was the username, and it did not have a password.

<img width="758" height="332" alt="image" src="https://github.com/user-attachments/assets/7ec637e6-de1c-4dcb-9b58-f22ba8ca01d3" />


**3. Flag Capture**

after the loggin using the ls command, I saw the directory and got the target file and flag.
<img width="785" height="206" alt="image" src="https://github.com/user-attachments/assets/c167585b-7719-4f33-ac11-327e0f15faae" />
